'use strict';

/**
 * @ngdoc service
 * @name webframe.config/disgDefaults
 * @description
 * # config/disgDefaults
 * Constant in the webframe.
 */

const ROW = 10;                   //表の行数

angular.module('webframe').constant('disgDefaults', Array(ROW).fill({}));