'use strict';

/**
 * @ngdoc service
 * @name webframe.config/fsecDefaults
 * @description
 * # config/fsecDefaults
 * Constant in the webframe.
 */

const ROW = 10;                   //表の行数

angular.module('webframe').constant('fsecDefaults', Array(ROW).fill({}));