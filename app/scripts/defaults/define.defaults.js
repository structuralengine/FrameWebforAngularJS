'use strict';

/**
 * @ngdoc service
 * @name webframe.config/defineDefaults
 * @description
 * # config/defineDefaults
 * Constant in the webframe.
 */
angular.module('webframe').constant('defineDefaults', Array(100).fill().map(u => ({})));