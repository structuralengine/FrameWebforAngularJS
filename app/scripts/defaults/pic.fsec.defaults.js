'use strict';

/**
 * @ngdoc service
 * @name webframe.config/picFsecDefaults
 * @description
 * # config/picFsecDefaults
 * Constant in the webframe.
 */

const ROW = 10;                   //表の行数

angular.module('webframe').constant('picFsecDefaults', Array(ROW).fill({}));