'use strict';

/**
 * @ngdoc service
 * @name webframe.config/picFsecDefaults
 * @description
 * # config/picFsecDefaults
 * Constant in the webframe.
 */
angular.module('webframe').constant('picFsecDefaults', Array(100).fill().map(u => ({})));