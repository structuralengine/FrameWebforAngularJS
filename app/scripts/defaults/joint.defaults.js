'use strict';

/**
 * @ngdoc service
 * @name webframe.config/jointDefaults
 * @description
 * # config/jointDefaults
 * Constant in the webframe.
 */
angular.module('webframe').constant('jointDefaults', Array(100).fill().map(u => ({})));