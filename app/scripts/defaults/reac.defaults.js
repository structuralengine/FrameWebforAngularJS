'use strict';

/**
 * @ngdoc service
 * @name webframe.config/reacDefaults
 * @description
 * # config/reacDefaults
 * Constant in the webframe.
 */

const ROW = 10;                   //表の行数

angular.module('webframe').constant('reacDefaults', Array(ROW).fill({}));