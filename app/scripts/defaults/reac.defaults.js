'use strict';

/**
 * @ngdoc service
 * @name webframe.config/reacDefaults
 * @description
 * # config/reacDefaults
 * Constant in the webframe.
 */
angular.module('webframe').constant('reacDefaults', Array(100).fill().map(u => ({})));