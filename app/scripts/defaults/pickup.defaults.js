'use strict';

/**
 * @ngdoc service
 * @name webframe.config/pickupDefaults
 * @description
 * # config/pickupDefaults
 * Constant in the webframe.
 */
angular.module('webframe').constant('pickupDefaults', Array(100).fill().map(u => ({})));