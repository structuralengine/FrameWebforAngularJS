'use strict';

/**
 * @ngdoc service
 * @name webframe.config/combineDefaults
 * @description
 * # config/combineDefaults
 * Constant in the webframe.
 */
angular.module('webframe').constant('combineDefaults', Array(100).fill().map(u => ({})));